### herdle   (/home/u/herdle)

— git —  main

— open PRs —
  #7     fix/7-x                        a fix                                     [her-aaaa]  ✓ ready to merge
  #8     feat/8                         a feature                                 [-]  ✗ checks failing · ⚠ ↑2 unpushed
  #9     spike/9                        a spike                                   [-]  —

— merged PRs needing cleanup —
  #6     old                            merged thing                              ⚠ local branch

— work in progress (in-flight tk + branches, not in a PR) —
  state               sync tk        branch                         feature                                  issues
  in-development      ✗    her-bbbb  feature-y                      the feature                              ↑1 unpushed
  -                   ·    -         orphan                                                                  no tk

— up next (open tk, not started) —
  planned             her-cccc  P2 next up

— design artifacts (specs / plans / validation) —
  her-aaaa  specs  2026-06-13-her-aaaa-x-design.md

sync: ✓ local==remote · ✗ differs (see issues) · · n/a — merged-PR & upstream-gone branches hidden, remote auto-pruned
PR status: ✓ ready to merge · ✗ conflicts · ✗ checks failing · ✎ changes requested · ⚠ blocked · — pending/draft/computing
lifecycle: - (not started) → designed → planned → in-development → pending-validation → validated
